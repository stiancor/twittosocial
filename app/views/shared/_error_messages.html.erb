<% if object.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-error">
        The form contains <%= pluralize(object.errors.count, "error") %>
      </div>
      <ul>
        <%= render 'shared/error_message', object: object %>
      </ul>
    </div>
<% end %>
<li>
  <%= link_to event.title, event %>
  <span class="username"> <%= '- ' if event.location %> <%= event.location %></span>
  <% if current_user.admin? || current_user?(event.user) %>
      | <%= link_to "delete", event, method: :delete, confirm: "Are you sure?" %>
  <% end %>
</li>